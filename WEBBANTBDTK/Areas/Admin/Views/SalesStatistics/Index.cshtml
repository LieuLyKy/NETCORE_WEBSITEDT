@{
    ViewData["Title"] = "Thống Kê doanh số";
 
}

<h1>@ViewData["Title"]</h1>





<div class="card">
 @*   <div class="card-body">
        <div class="row m-b-30">
            <div class="col-md-8">
                <div class="row">
                    <div class="col-md-6">
                        <div class="input-affix m-v-10">
                            <i class="prefix-icon anticon anticon-search opacity-04"></i>
                            <input id="keyword" name="keyword" type="text" class="form-control" placeholder="Tìm kiếm sản phẩm">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="input-affix m-v-10">
                            <select class="custom-select" id="txtCatID" name="txtCatID" style="min-width: 200px;" asp-items="ViewBag.DanhMuc">
                                <option selected>Chọn danh mục</option>
                                <option value="0">Chọn tất cả</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4 text-right">
                <a class="btn btn-primary" asp-area="Admin" asp-controller="ProductsAd" asp-action="Create"><i class="anticon anticon-plus-circle m-r-5"></i><span>Thêm sản phẩm</span></a>
            </div>
        </div>*@
        <div class="table-responsive">
            <table class="table table-hover e-commerce-table">
            <tr>
                <td>

                    <table>
                        <tr>
                            <th>Sản phẩm bán được</th>
                   
                            <th>Số lượng bán được</th>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ProductType</td>
                               
                                <td>@item.Quantity</td>
                            </tr>
                        }
                    </table>

                </td>

                <td>
                    <div class="btn btn-primary"><span>Tổng số sản phẩm bán được: @ViewData["TotalQuantity"]</span></div>
                    <div style="margin-left: 30px;" class="btn btn-primary"><span>Tổng giá thu được từ việc bán sản phẩm: @ViewData["TotalRevenue"]</span></div>
                </td>

                


            </tr>
                <tbody id="records_table">
                   
                       
                        
                           
                        
                    


                </tbody>
            </table>
            <div aria-label="Page navigation example">
               
            </div>
        </div>
    </div>

